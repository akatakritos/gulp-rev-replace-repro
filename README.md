Demonstrates https://github.com/jamesknelson/gulp-rev-replace/issues/69

Run `gulp` and notice in `dist/index.html` that the script tag at
the bottom references the `.map` file instead of the `.js` file.